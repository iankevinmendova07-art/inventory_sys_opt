# Copy these values into the hosting environment. Do not commit real secrets.
APP_ENV=production
DB_HOST=127.0.0.1
DB_NAME=inventory_sys_db
DB_USER=inventory_app
DB_PASS=replace-with-a-strong-password
